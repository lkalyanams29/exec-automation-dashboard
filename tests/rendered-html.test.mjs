import assert from "node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("QEI intelligence surface",async()=>{const s=await readFile(new URL("../app/Dashboard.tsx",import.meta.url),"utf8");for(const x of ["Ask QE","Mentor Mode","Evidence graph","Evaluation suite","INSUFFICIENT_EVIDENCE","ACCESS_RESTRICTED","Grounded faithfulness"])assert.match(s,new RegExp(x));assert.doesNotMatch(s,/KATALON_API_KEY\s*=/)});
test("environment credentials",async()=>{const s=await readFile(new URL("../backend/katalon/client.py",import.meta.url),"utf8");assert.match(s,/os\.getenv\("KATALON_API_KEY"\)/)});
test("Katalon corpus assets remain available",async()=>{const [data,route]=await Promise.all([readFile(new URL("../app/sample-data.ts",import.meta.url),"utf8"),readFile(new URL("../app/api/imports/route.ts",import.meta.url),"utf8")]);assert.match(data,/katalon_testops_sample_dataset\.csv/);assert.match(route,/summarizeCsv/)});
